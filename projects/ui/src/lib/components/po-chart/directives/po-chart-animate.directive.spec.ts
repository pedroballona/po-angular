import { PoChartAnimateDirective } from './po-chart-animate.directive';

describe('PoChartAnimateDirective', () => {
  it('should create an instance', () => {
    const directive = new PoChartAnimateDirective();
    expect(directive).toBeTruthy();
  });
});
